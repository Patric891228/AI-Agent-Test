# Python Add Project

This is a simple Python project with an addition function and a CLI program.

## Files

- `add.py`: provides the `add(a, b)` function
- `main.py`: reads two numbers from the command line and prints the result
- `requirements.txt`: dependency list

## Run

```bash
python main.py
```
